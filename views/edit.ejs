<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-700 p-10">
    <div class="w-full min-h-screen text-white">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5" href="/logout">Logout</a>
        </div>
        <h1 class="text-3xl mb-2">Hye <%= data.user.name %></h1>
        <h2 class="text-3xl"><span class="font-light">Edit your post</h2>
          
        <form action="/update/<%= data._id %>" method="post">
            <textarea placeholder="What's on your mind" name="content" class="w-1/3 p-3 block outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%= data.content %></textarea>
            <input type="submit" class="px-3 text-sm py-2 bg-yellow-500 text-black block rounded-md mt-2 w-40 cursor-pointer" value="Edit post">
        </form> 
        
    </div>
</body>
</html>

